[project]
name = "email-agent"
version = "0.1.0"
description = "A minimal, extensible Python starter for building an email agent."
requires-python = ">=3.13"
dependencies = []

[project.scripts]
email-agent = "email_agent.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
